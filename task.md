# Task Tracker

## Active Program
- Title: Residual end-state convergence after v4 refactor
- Status: in_progress
- Tracking:
  - Epic: `cng-z7u`
  - Active bead: `cng-z7u.4` (`in_progress`)
  - Planning notebook: `task_plan.md`, `findings.md`, `progress.md`

## Program Scope
- Convert the original `F1..F8` review into a practical end-state matrix: what is done, what is only mostly done, and what still needs execution.
- Finish the remaining `F5` persistence/reporting work: projection pipeline, typed schedule persistence, and projection-first reporting reads.
- Finish the remaining `F7`/`F8` residue: shrink the last oversized reporting/importer modules and remove noisy reporting-test output that still hides signal.

## Backlog
- `cng-z7u.1` (`P1`, closed): build a projection-backed reporting aggregate pipeline with explicit freshness, invalidation, and version ownership.
- `cng-z7u.2` (`P1`, closed): move reporting schedules to a typed projection-backed store instead of legacy `kv_store` ownership.
- `cng-z7u.3` (`P1`, closed): converge dashboard and export onto one projection-first reporting read-model contract.
- `cng-z7u.4` (`P2`, in progress): finish `ReportViewer` convergence and split the remaining reporting UI monolith.
- `cng-z7u.5` (`P2`, independent): continue `DataImporter.jsx` decomposition toward maintainable module boundaries.
- `cng-z7u.6` (`P3`, blocked by `cng-z7u.4`): silence `Recharts`/jsdom warning noise and harden the reporting harness.

## Current Slice
- `cng-z7u.1`, `cng-z7u.2`, and `cng-z7u.3` are now functionally landed: monthly aggregate projections carry freshness/ownership metadata, schedule persistence flows through typed projection stores, and both `/api/v4/reporting/view` and compact export resolve through the same reporting read-model builder.
- `cng-z7u.4` is now the active leverage point: `ReportViewer` has already shed the reporting controls/schedule seam plus overview widgets/company-summary table into dedicated modules, but still owns too much local shaping/detail-card orchestration to close the slice.
- The practical residual work is now concentrated in `F7`/`F8` (`ReportViewer`/`DataImporter` module size plus noisy reporting verification), not in backend reporting contract drift.
- Recently closed slices retained below:
- `cng-cff`: the broad backend diagnostic path moved from `14` mixed failures to green by first reducing stale fixture/assertion drift to `6`, then isolating the last rule-selection regression to `1`, and finally closing it end-to-end.
- `cng-cff`: root cause was `server/reportingRuleSelection.js` rejecting legacy/simple rule-set snapshots that lacked `groups`; `getRulesValue()` then fell back to `SHARED_DEFAULT_RULES`, which masked agency-specific ECUS license exclusions.
- `cng-cff`: `tests/server.api.test.js` now uses canonical roster/ECUS fixtures plus the typed `/api/storage/decl_rows_v1` write path, and `tests/reportingRuleSelection.test.js` now locks the legacy rule-selection contract with dedicated regression coverage.
- Historical blueprint slices retained below:
- `cng-i6h.4`: `apps/api` is no longer only a workspace shell; `apps/api/src/apiRuntimeConfig.js`, `apps/api/src/startApiServer.js`, and `apps/api/src/cli.js` now own the first canonical launcher seam for the `server-v4` runtime.
- `cng-i6h.4`: the launcher no longer depends on `process.cwd()` for the SQLite path. Repo-root DB resolution and host/port env parsing are now package-owned, so `apps/api` can point at `server/data/storage.sqlite` consistently instead of assuming the monolith root as the working directory.
- `cng-i6h.4`: root script `build:server-v4` plus `tsconfig.server-v4.build.json` now emit `dist/server-v4`, letting `apps/api` load compiled runtime output directly instead of staying a shell that only works under test-runner transpilation.
- `cng-i6h.4`: reporting persistence now moves through an explicit runtime adapter seam instead of `server-v4/src/modules/reporting/ReportingRepository.ts` opening the legacy SQLite file directly. The current SQLite projection implementation remains the compatibility backend, but ownership is now injectable from the canonical runtime path.
- `cng-i6h.4`: `apps/api` now owns explicit persistence-mode guardrails (`sqlite-dual-write` vs `postgres`) and fails fast when `postgres` mode is requested without a URL or without a real adapter implementation. This keeps migration intent explicit instead of silently booting the SQLite compatibility path.
- `cng-i6h.4`: monolith reporting aggregate orchestration now lives in `server/reportingAggregateRuntime.js`; `server/index.js` delegates reporting view freshness, observability payloads, monthly aggregate materialization, and source-triggered aggregate refresh through that seam instead of keeping the whole reporting runtime block inline.
- `cng-i6h.4`: reporting schedule read/save/delete flow now also lives behind `server/reportingScheduleRuntime.js`; `server/index.js` keeps auth/audit wrappers but delegates schedule listing, default aggregate readiness, and schedule persistence through the new runtime seam.
- `cng-i6h.4`: the generic `kv_store` dual-write/bootstrap lifecycle now also lives behind `server/runtimeStorageLifecycle.js`; `server/index.js` delegates storage fan-out, bootstrap snapshot assembly, and test-reset hydration through the new seam instead of keeping those persistence side effects inline.
- `cng-i6h.4`: generic `/api/storage` mutation orchestration now also lives behind `server/storageRouteRuntime.js`; `server/index.js` keeps validation/permission/protected-key/HTTP wrappers but delegates storage PUT/PATCH/DELETE side effects (`decl_rows_v1`, `ecus_sync_config_v1`, `co_tax_code_config_v1`, `co_discrepancy_config_v1`) through the new runtime seam.
- `cng-i6h.4`: `server-v4` and `apps/api` now carry the first real Postgres-backed reporting projection adapter. `server-v4/src/persistence/reportingProjectionPostgres.ts` owns Postgres persistence for `reporting_projections`, `runtimePersistence` no longer hard-throws in `postgres` mode, reporting projection reads/writes are async end-to-end, and `buildV4App` plus `apps/api/src/startApiServer.js` now propagate/dispose persistence lifecycle explicitly on shutdown.
- `cng-i6h.4`: `server-v4` `postgres` mode no longer keeps an internal `SqliteBusinessSnapshotReader` alive just to satisfy untouched interface calls. `server-v4/src/persistence/noopBusinessSnapshotReader.ts` now marks the compatibility surface as `relational-store`, the Postgres async readers inherit that source kind, and `server-v4/src/app/v4-rollout-status.ts` no longer treats a missing legacy SQLite file as a hard blocker when the runtime is already on the relational-store path.
- `cng-i6h.4`: `server-v4` now mounts the first standalone `kpi-adjustments` route family beyond reporting-input transport. `server-v4/src/modules/kpi-adjustments/kpiAdjustmentsRoutes.ts` plus its repository/service/controller seam expose filtered adjustment reads at `/api/v4/kpi-adjustments`, and `buildV4App` now treats that module as implemented so rollout coverage advances from 5 to 6 mounted modules.
- `cng-i6h.4`: rollout truthfulness now uses actual mounted runtime route coverage instead of inferring implemented progress from the full module catalog. `server-v4/src/app/runtimeRouteCoverage.ts` feeds `buildV4App()` and `server-v4/src/app/v4-rollout-status.ts`, so read-only slices like `kpi-adjustments` no longer overstate implemented mutation coverage or route totals.
- `cng-i6h.4`: `server-v4` now mounts `hq-agencies` as a real canonical read surface with `/api/v4/hq-agencies` plus `/api/v4/hq-agencies/history`. The new async reader seam and Postgres adapter read `tax_code_agency_bindings`, `tax_code_agency_binding_agents`, and `tax_code_agency_binding_events` first, then fall back to the SQLite `kv_store` compatibility payloads, so rollout coverage advances again and only `auth` remains scaffolded.
- `cng-i6h.4`: targeted verification is green for both new monolith reporting seams (`tests/reportingAggregateRuntime.test.js`, `tests/reportingScheduleRuntime.test.js`, and `tests/server.api.test.js -t "V4 reporting"`). The broad `tests/server.api.test.js` mega-bundle still fails in the same unrelated 14 non-reporting buckets and remains diagnostic rather than a closure gate for this phase.
- `cng-i6h.4`: `kpi-adjustments` now also owns a real session-backed write surface in `server-v4`. `server-v4/src/modules/kpi-adjustments/*` now serves `GET/PUT /api/v4/kpi-adjustments/settings`, `POST /api/v4/kpi-adjustments`, and `PATCH /api/v4/kpi-adjustments/:adjustmentId` through the new `authSessionContext` plus `adjustmentsStore` runtime contract, with SQLite/Postgres write adapters backing settings and adjustment mutations. Default rollout metrics now settle at `8/8` implemented modules, `0` scaffold modules, `5` read-only modules, `3` read-write modules, `24` implemented routes, `9` implemented mutation routes, `readiness=ready`, and `currentStage=cutover-ready`.
- `cng-i6h.4`: `kpi-rules` now also owns a real session-backed write/settings surface in `server-v4`. `server-v4/src/modules/kpi-rules/*` now serves `POST /api/v4/kpi-rules` and `POST /api/v4/kpi-rules/:ruleSetId/activate` through the new `kpiRulesStore` runtime contract plus cookie-session auth resolution, with SQLite/Postgres write adapters backing canonical draft creation and activation. Default rollout metrics now settle at `8/8` implemented modules, `0` scaffold modules, `4` read-only modules, `4` read-write modules, `26` implemented routes, `11` implemented mutation routes, `readiness=ready`, and `currentStage=cutover-ready`.
- `cng-i6h.4`: `hq-agencies` now also owns a real session-backed write/history surface in `server-v4`. `server-v4/src/modules/hq-agencies/*` now serves `POST /api/v4/hq-agencies` and `DELETE /api/v4/hq-agencies/:taxCode` through the new `hqAgenciesStore` runtime contract plus cookie-session auth resolution, with SQLite/Postgres write adapters backing binding upsert/delete and history-event persistence. Default rollout metrics now settle at `8/8` implemented modules, `0` scaffold modules, `3` read-only modules, `5` read-write modules, `28` implemented routes, `13` implemented mutation routes, `readiness=ready`, and `currentStage=cutover-ready`.
- `cng-i6h.4`: `declarations` now also owns a real session-backed mutation and event-history surface in `server-v4`. `server-v4/src/modules/declarations/*` now serves `PATCH /api/v4/declarations/:declarationId` plus `GET /api/v4/declarations/:declarationId/events` through the new `declarationsStore` runtime contract plus cookie-session `importEdit`/`accountManage` enforcement, preserving reviewed-declaration admin override behavior. Default rollout metrics now settle at `8/8` implemented modules, `0` scaffold modules, `2` read-only modules, `6` read-write modules, `30` implemented routes, `14` implemented mutation routes, `readiness=ready`, and `currentStage=cutover-ready`.
- `cng-i6h.5`: `server-v4` `postgres` mode no longer silently constructs the `hq-agencies` SQLite compatibility reader. `apps/api` now exposes `KPI_API_POSTGRES_LEGACY_SQLITE_FALLBACK`, `runtimePersistence` defaults to a `NoopHqAgenciesAsyncReader`, and the legacy SQLite path is only re-enabled through an explicit rollback flag.
- `cng-i6h.5`: `apps/api` and `server-v4` no longer synthesize or carry a default SQLite `dbFile` in `postgres` mode. `resolveServerV4Config()` / `loadApiRuntimeConfig()` now return `dbFile: null` unless the runtime is on `sqlite-dual-write` or the explicit rollback flag is enabled, and `runtimePersistence` now requires `KPI_API_DB_FILE` only for those legacy-file paths.
- `cng-i6h.5`: rollout health now treats an unset legacy DB path as `not configured` / `not-required` for the relational-store path, so `/api/v4/health` and `/api/v4/meta/rollout` no longer imply that Postgres mode still owns a hidden SQLite file by default.
- `cng-i6h.5`: inventory across `adjustments`, `declarations`, `kpi-rules`, `mst-assignments`, and `teams` confirmed that the default Postgres runtime no longer hides a runtime-wide SQLite snapshot reader; the remaining residue is now reader-local and uneven, with `declarations` and `adjustments` still carrying canonical-table -> snapshot-row -> compatibility-reader fallback chains while `teams` remains the deeper broader seam.
- `cng-i6h.5`: `server-v4/src/modules/declarations/postgresDeclarationAsyncReader.ts` and `server-v4/src/modules/kpi-adjustments/postgresAdjustmentAsyncReader.ts` now stop at canonical relational rows and fall directly to the compatibility reader when canonical rows are absent. Postgres mode no longer consults `declaration_snapshot_rows` or `adjustment_snapshot_rows` as intermediate transport fallbacks.
- `cng-i6h.5`: `server-v4/src/modules/teams/postgresTeamRosterAsyncReader.ts` no longer gates the canonical roster path on `team_roster_state`. The Postgres reader now trusts `teams` + `team_members` as the primary source, returns a canonical empty roster in `relational-store` mode when those tables are empty, and only falls back to compatibility data in `dual-write` mode or on query failure.
- `cng-i6h.5`: `server-v4` teams now owns a real authenticated write path. `server-v4/src/modules/teams/teamRosterDocument.ts` centralizes roster normalization, `teamsStore.ts` defines the runtime persistence contract, and `PUT /api/v4/teams` now persists through explicit SQLite/Postgres stores instead of relying on legacy `/api/storage` side effects.
- `cng-i6h.5`: `runtimePersistence` now injects `teamsStore` for both SQLite dual-write and Postgres mode, `buildV4App()` wires that store into the teams router, and rollout truth advances again to `1` read-only module, `7` read-write modules, `31` implemented routes, and `15` implemented mutation routes.
- `cng-i6h.5`: `server-v4/src/types/better-sqlite3.d.ts` now matches the runtime `transaction()` API, which keeps the new SQLite teams store on the same type-safe build boundary as the rest of `server-v4`.

## Completed Context
- `cng-i6h.4`: advance canonical apps-api and Postgres cutover (`closed`)
- `cng-i6h.5`: retire residual SQLite compatibility and deepen canonical Postgres ownership (`closed`)
- `cng-i6h`: Complete blueprint end-state follow-through (`closed`)
- `cng-i6h.2`: retire remaining compatibility fallbacks (`closed`)
- `cng-i6h.3`: stabilize broad verification gates (`closed`)
- `cng-zss`: Comprehensive KPI v4 review and refactor blueprint (`closed`)
- `cng-7c8.3`: Phase 1 auth and session model cleanup (`closed`)
- `cng-7c8.4`: Phase 1 PostgreSQL target schema and migration map (`closed`)
- `cng-7c8.5`: Phase 1 backend modular monolith scaffold in TypeScript (`closed`)
- `cng-7c8.6`: Phase 2 declarations, MST, teams, and rules migration (`closed`)
- `cng-7c8.7`: Phase 2 reporting and KPI aggregation redesign (`closed`)
- `cng-7c8.8`: Phase 2 UI shell and design system v4 (`closed`)
- `cng-7c8.9`: Phase 2 import workflow and data-heavy UI optimization (`closed`)
- `cng-7c8.1`: Phase 0 security boundary hardening (`closed`)
- `cng-7c8.2`: Phase 0 baseline recovery and toolchain stabilization (`closed`)
- `cng-7c8.10`: Phase 3 ECUS bridge extraction (`closed`)
- `cng-7c8.11`: Phase 3 observability, QA matrix, and staged rollout (`closed`)
- `cng-7c8`: KPI v4 re-architecture program (`closed`)
- `cng-3or.1`: Phase A PostgreSQL runtime persistence and dual-write cutover (`closed`)
- `cng-3or.3`: Phase B delivery-shape split into apps and shared packages (`closed`)
- `cng-3or.2`: Phase C standalone Windows ECUS bridge service (`closed`)
- `cng-3or.4`: Phase D operator shell end-state and heavy-workflow redesign (`closed`)
- `cng-3or.5`: Phase E reporting analytics and observability end-state (`closed`)
- `cng-3or`: KPI v4 full-cutover continuation program (`closed`)
- `cng-4or.1`: Phase A server-v4 ESLint and CI hardening (`closed`)
- `cng-4or.2`: Phase B broaden server-v4 ESLint coverage (`closed`)
- `cng-4or.3`: Phase C wire hardening gates into CI (`closed`)
- `cng-4or`: Post-cutover hardening program (`closed`)
- Main review artifact: `docs/system-v4-review-plan.md`

## Notes
- This repository is a copy of the production project, so implementation here can be stronger-handed than the production rollout sequence.
- New modules should stay under roughly 800 LOC where feasible.
- Every new module needs tests, and any changed behavior should update tests accordingly.
- `cng-7c8` closed the first execution program cleanly, but `docs/system-v4-review-plan.md` still leaves long-horizon end-state work; `cng-3or` tracks that continuation explicitly instead of reusing the completed epic.
- Bead ordering in the continuation program is intentionally non-sequential by suffix because the child tasks were created in different order: `cng-3or.1` is Phase A, `cng-3or.3` is Phase B, and `cng-3or.2` is Phase C.
- `cng-3or.1` completed with a full typed runtime seam for the original six hot-path keys: declarations/MST/rules/adjustments now dual-write into `server/businessSnapshotSqlite.js`, teams dual-write into `server/teamRosterSqlite.js`, and report schedules/default monthly aggregates stay on the relational `reporting_projections` boundary.
- `server-v4` hot business modules now read through `BusinessSnapshotReader`, and the runtime prefers typed snapshots/projections over stale blob payloads for declarations, MST assignments, teams, KPI rules, adjustments, reporting schedules, and reporting view composition.
- `/api/v4/meta/rollout` now reports persistence source `dual-write`, so rollout metadata reflects that the runtime is no longer legacy-blob-first for these hot paths.
- The last real Phase A regression was declaration normalization idempotence: `normalizeDeclarationRow()` now returns the original row when no field changes, preventing recursive write-on-read loops when typed declaration snapshots feed reporting refreshes.
- `cng-3or.3` completed with the first real delivery-shape extraction: the repo now has `pnpm-workspace.yaml`, app shells under `apps/web`, `apps/api`, and `apps/ecus-bridge`, plus package source under `packages/domain`, `packages/api-client`, and `packages/ui`.
- Phase B moved stable shared implementation into package source while preserving current entrypoints as compatibility shims: `shared/*`, `src/shared/*`, `src/lib/reportingClient.js`, and `src/components/designSystem/*` now forward to package-owned code instead of staying as the canonical implementation.
- `cng-i6h.2` finished by retiring the last runtime fallback seam in reporting: `server-v4` reporting schedules and monthly aggregate/default aggregate reads now use `reporting_projections` only, and monolith/test seed helpers materialize those projection rows directly instead of preserving blob-first reads.
- `cng-i6h.3` finished by formalizing repo smoke gates: `test:smoke:backend-core`, `test:smoke:frontend-core`, `test:smoke:core`, and `verify:smoke:core` now capture the trustworthy broad verification path, while the known-noisy `server.seed + server.api` mega-bundle stays diagnostic rather than a merge gate.
- Phase C now has a live token-protected core contract for the extracted ECUS seam: `/api/v4/declarations/imports/ecus-config|ecus-preview|ecus-commit` is wired in the monolith and backed by `server/ecusBridgeMutations.js`, but the standalone Windows bridge host plus DPAPI/SQL isolation are still pending.
- Phase C now also has a package-local host controller in `apps/ecus-bridge/src/bridgeHost.js`, so the bridge package owns health/status/busy and single-flight sync semantics around the extracted runtime even before the final HTTP/Windows service bootstrap lands.
- Phase C now also has a package-local HTTP boundary and composition root in `apps/ecus-bridge/src/bridgeHttpServer.js` and `apps/ecus-bridge/src/bridgeBootstrap.js`; the remaining gap is the final executable Windows service/worker entrypoint plus full SQL/DPAPI isolation.
- Phase C now also has an executable standalone bridge entrypoint in `apps/ecus-bridge/src/bridgeCli.js` plus a package-level `start` script in `apps/ecus-bridge/package.json`, so the bridge package can boot end to end with graceful shutdown; the remaining gap is Windows service registration/lifecycle ownership plus fully moving SQL Server and DPAPI custody off the monolith path.
- Phase C now also has a Windows service lifecycle wrapper in `apps/ecus-bridge/src/bridgeWindowsServiceCli.js` plus package scripts `service:install`, `service:remove`, and `service:status`, so service registration/status/delete flows are now owned by the bridge package instead of remaining implicit operator knowledge.
- Phase C is now complete: `apps/ecus-bridge/src/bridgeSecureCredentials.js` and `apps/ecus-bridge/src/sqlBridge.js` are the canonical DPAPI + SQL ownership boundary for the standalone bridge, `server/ecus/secureCredentials.js` and `server/ecus/sqlBridge.js` are compatibility shims only, and `docs/operations/ecus-bridge-service.md` records the final env/token/retry/health runbook.
- Phase D is now in progress: the first shell slice promotes `src/lib/appShellNavigation.js` from a flat tab list into grouped navigation sections, and `src/components/appShell/AppShellFrame.jsx` now provides a real domain sidebar plus workflow summary shell around `KPICalculator` without changing the underlying tab content contracts yet.
- The first Phase D slice deliberately stops at shell/navigation composition: `KPICalculator` still owns the heavy workflow content, while `MST`/`import`/`reporting` deep redesign remains for later `cng-3or.4` slices.
- Phase D is now complete: `src/components/appShell/appShellWorkflowState.js` and `src/components/appShell/AppShellWorkflowGuide.jsx` give the shell a real workflow contract, `CommandCenter` can now be launched via the command bus, and `KPICalculator` routes MST, KPI adjustments, and report center through dedicated workflow wrappers instead of one flat operator surface.
- `src/App.jsx` now preserves `{ tab, focus }` navigation intent so shell actions and Command Center results can land users on stable workflow anchors rather than only switching tabs.
- A broad App-level smoke rerun via `tests/e2e.login-import.test.jsx` did not terminate cleanly enough to use as a trusted gate for this slice; Phase D closure is based on the targeted shell/auth bundles that cover the touched surfaces directly.
- The first packageized slices are intentionally low-risk and high-leverage:
  - `packages/domain` owns reporting math, KPI adjustment helpers, license summary logic, and core shared domain utilities such as `accountRoles`, `co`, `declSearch`, `defaultRules`, `format`, and `sampleDeclarations`.
  - `packages/api-client` owns the reporting client/view-model builder.
  - `packages/ui` owns the shell primitives and data-table primitives used by the v4 shell surfaces.
- Current trusted baseline commands:
  - `pnpm healthcheck`
  - `pnpm lint`
  - `pnpm exec vitest run tests/server.api.test.js`
- Phase 1A completed:
  - Frontend auth now relies on cookie-backed session requests only.
  - Legacy `kpi_session_token` storage and `Authorization: Bearer ...` injection were removed from the client path.
  - Demo and frontend test mocks were aligned with the tokenless client model.
  - Backend auth endpoints no longer echo session `token` values in JSON responses; the cookie session is now the only runtime auth credential returned to the browser.
- Clean RBAC enforcement points now documented in the notebook:
  - `requireAuthenticated()` is the session gate.
  - `requireAccountManage()` protects account lifecycle routes.
  - `verifyStoragePermission()` protects `/api/storage/:key` by key-to-permission mapping.
  - Specialized guards (`requireAdminSyncManage`, `requireAlertsManage`, `requireAuditView`, `requireAiAssistUsage`, `requireAiAssistManage`, `requireRulesManage`, `requireNotificationAccess`, etc.) stay at route entry, not in client code.
- Final completion:
  - `cng-7c8.9` closed with the importer split under the project soft cap, workbook workerization, and the guided file/sync review-save workflow.
  - `cng-7c8.10` closed with `server/ecus/bridgeService.js` as the ECUS seam for SQL Server health, declaration fetches, connection summary, and account-sync idle waiting; `server/index.js` now delegates through that bridge instead of owning the platform-specific logic inline.
  - `cng-7c8.11` closed with rollout-readiness metadata in `server-v4/src/app/v4-rollout-status.ts`, richer `/api/v4/health`, a dedicated `/api/v4/meta/rollout` endpoint, and operator docs in `docs/operations/v4-qa-matrix.md` plus `docs/operations/v4-rollout-plan.md`.
  - The `cng-7c8` epic is now complete; all child phases are implemented, verified, and synced in the notebook.
- `cng-7c8.7` now also tightens the export boundary: `/api/reports/export` can resolve compact `reporting-v4` payloads server-side from the reporting source snapshot, so the browser no longer needs to post raw `staffList`/`teamList`/`summary` blobs for the new path and export audits keep compact filters.
  - Current `cng-7c8.7` slice: `ReportViewer` migration work exposed a frontend-only read-path bug where `loadRuleSets()/loadRules()` re-persisted an already-valid rule collection and rebroadcast `RULES_KEY`; that loop is now fixed at the source, with regression coverage added so reporting reads stay side-effect free.
  - Current `cng-7c8.7` slice: reporting read models now ship precomputed company-summary payloads (`summary.companies`, `staff.items[].companies`, `teams.items[].companies`) from both the monolith shim and `server-v4`, and `ReportViewer` now consumes those payloads instead of calling `aggregateByCompany()` in the browser.
  - Current `cng-7c8.7` slice: `src/lib/reportingClient.js` no longer synthesizes a top-level `report.rows` snapshot by deduping every `staff.items[].rows` payload, because the reporting UI no longer consumes that browser-side assembly path.
  - Current `cng-7c8.7` slice: `src/lib/reportingClient.js` also no longer mirrors collection-level `keysHash` metadata into the browser view model; the client reporting model now stays focused on `list` and `byKey`, while hash/version metadata remains a server-contract concern until a real UI consumer exists.
  - Current `cng-7c8.7` slice: `src/lib/reportingClient.js` now trims unused trend metadata too; `teamSeries` and `topTeams` remain part of upstream reporting payloads but are no longer copied into the browser-normalized model because `ReportViewer` only consumes `series` and `comparison`.
  - Current `cng-7c8.7` slice: staff/team item normalizers no longer spread arbitrary server payload fields into the browser model. `src/lib/reportingClient.js` now emits an explicit shape for reporting entities, keeping only fields that current UI paths actually consume.
  - Current `cng-7c8.7` slice: top-level `summary` is now normalized through an explicit client shape too; browser state keeps only the KPI summary metrics the current dashboard actually renders, instead of mirroring arbitrary server summary fields.
  - Current `cng-7c8.7` slice: `report.rules` is now normalized through an explicit client shape as well. Browser state keeps the rule identity/display fields and `license.exclude.codes` needed by the current reporting UI, but strips unrelated nested rule metadata.
  - Current `cng-7c8.7` slice: `adjustments` normalization now also lives in `reportingClient`. The client model accepts legacy `totals` / `totalsByCategory` aliases and numeric coercion there, so `ReportViewer` no longer needs to re-normalize that branch before rendering.
  - Current `cng-7c8.7` slice: approved adjustment rows are now normalized into a render-ready flat shape in `reportingClient`, and `ReportViewer` no longer dereferences nested `item.adjustment?...` fields for that table.
  - Current `cng-7c8.7` slice: `ReportViewer` now also reads `report.adjustments` directly instead of rebuilding a local `adjustmentsReport` wrapper, and the last two unused locals in that path were removed so targeted lint on the reporting browser path is clean again.
  - Current `cng-7c8.7` slice: staff/team item normalizers now also emit `licenseSummary` and `adjustmentTotals`, so `ReportViewer` no longer rebuilds those two view-only branches in each detail card from raw `stats.licenseCodes` and `adjustmentSummary`.
  - Current `cng-7c8.7` slice: staff/team item normalizers now also emit `adjustmentMetrics` (`totalPoints`, `entryCount`, `positive`, `negative`, `neutral`), so detail cards no longer rescan raw `rows` to compute adjustment totals and breakdown counts on every render.
  - Current `cng-7c8.7` slice: top-level `report.adjustments` now also emits `totalsList`, so `ReportViewer` no longer calls `toAdjustmentTotalsArray()` for the main adjustments summary branch either.
  - Current `cng-7c8.7` slice: team member rows and staff/team summary tables now read normalized `licenseSummary` strings from the reporting client model, so `ReportViewer` no longer joins `stats.licenseCodes` inline in those three table paths.
  - Current `cng-7c8.7` slice: staff/team detail `rows[]` now also carry `licenseSummary` and `licenseExcludedSummary`, so the two detail tables no longer join raw `licenseCodes` / `licenseExcludedCodes` arrays inline while rendering.
  - Current `cng-7c8.7` slice: `report.rules` now also emits `licenseExcludedSummary`, so the rule-summary panel no longer joins `license.exclude.codes` inline in `ReportViewer`.
- Current `cng-7c8.7` slice: `ReportViewer` now also consumes `/api/v4/reporting/schedules` aggregate-status metadata and remote schedule summaries, while still overlaying local schedule edits during the transition.
- Current `cng-7c8.7` slice: schedule local-overlay/save/delete/subscription plumbing now lives in `src/lib/reportingClient.js`, so `ReportViewer` no longer imports direct `get/save/deleteReportSchedule` helpers from `store.js` for that path.
- Current `cng-7c8.7` slice: reporting schedule writes now go through explicit `POST /api/v4/reporting/schedules` and `DELETE /api/v4/reporting/schedules/:id` reporting endpoints in both the monolith shim and `server-v4`, while generic `PUT`/`DELETE /api/storage/kpi_report_schedule_v1` writes are blocked so the schedule mutation path no longer falls back to the storage API.
- Current `cng-7c8.7` slice: `ReportViewer` now subscribes to the shared `reportingClient` schedule cache, so save/delete update the canonical reporting schedule view model without store-backed overlays or component-local reload/version bumps.
- Current `cng-7c8.7` slice: the earlier `server-v4` vite-node loader workaround is now retired because reporting math lives in shared modules; `server-v4/src/legacy/legacy-report-bridge.ts` is only a thin wrapper over the shared backend bridge now.
- Current `cng-7c8.7` slice: both the monolith shim and `server-v4` now expose `/api/v4/reporting/view`, and `src/lib/reportingClient.js` routes `fetchReportingViewModel()` through that single combined boundary instead of assembling dashboard state from three browser-side requests.
- Current `cng-7c8.7` slice: `server/reportExportPayloads.js` now resolves compact `reporting-v4` exports from `buildReportingReadModels()` too, so compact export materialization and dashboard reads share the same reporting read-model contract instead of rebuilding separate report snapshots inline.
- Current `cng-7c8.7` slice: the monolith now routes `kpi_report_schedule_v1` and monthly aggregate snapshot key access through `server/reportingProjectionStore.js` instead of inlining those `kv_store` reads/writes throughout `server/index.js`, which narrows the persistence seam for the next projection-table cutover.
- Current `cng-7c8.7` slice: `reporting_projections` is now the real persistence store for KPI schedules plus active/default monthly aggregate snapshots in both the monolith and `server-v4`, while reads still fall back to legacy `kv_store` for older seeded data.
- Current `cng-7c8.7` slice: refresh/materialize paths now keep active/default monthly aggregate snapshots inside `reporting_projections`, and `ReportViewer` baseline rule comparisons now also reuse `/api/v4/reporting/view` instead of fanning out to `/api/v4/reporting/summary`.
- Current `cng-7c8.7` slice: compatibility `/api/v4/reporting/summary|staff|teams` routes in the monolith, `server-v4`, and the mock API harness now all slice from the same `/api/v4/reporting/view` bundle instead of rebuilding parallel read models, and the dead `fetchReportingSummary()` client helper has been removed.
- Current `cng-7c8.7` slice: backend reporting math now lives under shared reporting modules (`shared/reportingLegacyMath.js`, `shared/reportingDateRanges.js`, `shared/reportingCompanyAggregation.js`); `server/legacyReportingBridge.js` imports those shared modules directly, while `src/lib/reports.js` is now only a compatibility wrapper for existing frontend callers.
- Current `cng-7c8.7` slice: shared reporting math is now also detached from `src/lib/store.js` and `src/lib/rules.js`; `shared/reportingLegacySupport.js` and `shared/reportingKpiComputation.js` carry the pure normalization/roster/KPI helpers, while `shared/reportingLegacyMath.js` only consumes those shared seams.
- Current `cng-7c8.7` slice: `reporting_projections` rows now persist typed metadata columns too (`scope_key`, `range_from`, `range_to`, `query_key`, `entry_count`) alongside the JSON payload, so reporting snapshots are no longer opaque blobs at the SQLite row level.
- Current `cng-7c8.7` slice: the compatibility `/api/v4/reporting/summary|staff|teams` endpoints are now fully retired in both runtimes, and the monolith now returns JSON `404` for unmatched `/api/*` requests instead of silently falling through to the SPA `index.html` fallback.
- Current `cng-7c8.7` slice: projection persistence now materializes entry rows into dedicated relational tables too: schedules go to `reporting_schedule_projection_entries`, and monthly aggregate snapshots go to `reporting_monthly_aggregate_projection_entries`, while the top-level projection payload JSON remains in place as the compatibility/read-through cache.
- Status: `cng-7c8.7` is complete. Further reporting work should land as later-phase consumer/performance slices rather than more Phase 2 boundary cleanup.
- Current `cng-7c8.8` slice: shell navigation metadata now lives in `src/lib/appShellNavigation.js`, and both `KPICalculator` and `CommandCenter` consume that shared registry instead of maintaining separate tab label/visibility maps.
- Current `cng-7c8.8` slice: Command Center now covers all visible shell tabs (`mst`, `hq`, `rules`, `audit`, `export-audit` included), so keyboard/global search navigation is aligned with the actual tab shell instead of a partial hardcoded subset.
- Current `cng-7c8.8` slice: `CommandCenter` command rows no longer render a nested `button` inside another `button`; the row surface is now a keyboardable `role="button"` container, which removes invalid markup from this high-traffic shell surface.
- Current `cng-7c8.8` slice: shared section/search primitives now live in `src/components/designSystem/shellPrimitives.jsx`, giving the UI shell a reusable seam for list headers, filter rows, and action rails instead of one-off flex/input markup.
- Current `cng-7c8.8` slice: `AccountManager` now adopts that shell seam for both the create-account and account-list surfaces, and the account table now carries an explicit accessible label/caption plus a denser sticky-header presentation.
- Current `cng-7c8.8` slice: `CommandCenter` now exposes real dialog semantics (`role="dialog"`, labelled searchbox, dialog trigger metadata) while reusing the same search-field primitive as list surfaces.
- Current `cng-7c8.8` slice: `MSTAssignment` now uses the shared shell primitives for the main assignment/filter/history surfaces, and its admin regression coverage exercises the labelled shell path instead of bespoke toolbar markup.
- Current `cng-7c8.8` slice: `ReportViewer` now uses the same shell chrome for the KPI control panel and reporting schedule surfaces, keeping reporting controls on the shared section/search primitives rather than local one-off headers.
- Current `cng-7c8.8` slice: `DataImporter` now wraps the main declaration control region in `SectionSurface`, `SectionHeader`, and `SearchField`, with an explicit labelled filter form plus main-table caption/`aria-label`.
- Current `cng-7c8.8` slice: wide-shell verification is stable too; `tests/commandCenter.test.jsx` now waits for the component's async command dispatch, and the HQ admin e2e no longer mistakes the shell search field for the editable add-row inputs.
- Status: `cng-7c8.8` is complete. The next phase should move to `cng-7c8.9` import workflow decomposition/redesign rather than further shell-only cleanup.
- Current `cng-7c8.9` slice: the importer multi-select action rail now lives in `src/components/dataImporter/DataImporterSelectionActions.jsx`, with targeted regression coverage in `tests/dataImporterSelectionActions.test.jsx`; this is the first real behavior seam extracted out of `DataImporter.jsx` after the shell rollout.
- Current `cng-7c8.9` slice: the ECUS preview/sync action panel now also lives in `src/components/dataImporter/DataImporterSyncPreviewPanel.jsx`, so the importer phase has started separating operator preview workflow chrome from the remaining monolithic sync/config container.
- Current `cng-7c8.9` slice: the deleted-declarations dialog now lives in `src/components/dataImporter/DataImporterDeletedRowsDialog.jsx`, so deleted-row review/history UI is no longer embedded directly in the top-level importer render tree.
- Current `cng-7c8.9` slice: the import preview summary now also lives in `src/components/dataImporter/DataImporterImportPreviewSummary.jsx`, so pre-import stats, invalid-row samples, inserted-row samples, and new-business badges are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the column-configuration modal now also lives in `src/components/dataImporter/DataImporterColumnConfigDialog.jsx`, so visible-column selection and admin-sensitive column gating are no longer embedded directly in the top-level importer render tree.
- Current `cng-7c8.9` slice: the duplicate-diff modal now also lives in `src/components/dataImporter/DataImporterDuplicateDiffDialog.jsx`, so pairwise field comparison between duplicate declarations is no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the duplicate-review workflow dialog now also lives in `src/components/dataImporter/DataImporterDuplicateReviewDialog.jsx`, so the largest remaining duplicate-review render branch has been pulled out of `DataImporter.jsx` while parent state and callbacks stay in place for now.
- Current `cng-7c8.9` slice: the save/edit grid toolbar controls now also live in `src/components/dataImporter/DataImporterGridToolbarControls.jsx`, so server-search status, view-mode switching, freeze/card-layout controls, page-size controls, deleted-row toggle, paging, and save action are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the card-results branch now also lives in `src/components/dataImporter/DataImporterCardResults.jsx`, so card-mode row rendering, card-level edit controls, history panel, and empty state are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the table-body branch now also lives in `src/components/dataImporter/DataImporterTableBody.jsx`, so table-mode row rendering, inline grid edit controls, row history expansion, row save/delete actions, and empty state are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the table-header branch now also lives in `src/components/dataImporter/DataImporterTableHeader.jsx`, so grid column labels, optional column gating, frozen-header styling, and resize-handle rendering are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the table wrapper/composition branch now also lives in `src/components/dataImporter/DataImporterTableResults.jsx`, so `DataImporter.jsx` no longer renders the table shell, caption, wrapper classes, and table-mode composition inline.
- Current `cng-7c8.9` slice: the result-mode switch and KPI footnote now also live in `src/components/dataImporter/DataImporterResultsPanel.jsx`, so `DataImporter.jsx` no longer branches directly between table/card result surfaces inline.
- Current `cng-7c8.9` slice: the monitoring/alerting branch now also lives in `src/components/dataImporter/DataImporterMonitoringPanel.jsx`, so the admin-only `Đối soát C/O` and `Cảnh báo tờ khai thiếu thông tin` surfaces are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the C/O code configuration branch now also lives in `src/components/dataImporter/DataImporterCoCodeConfigPanel.jsx`, so the admin-only whitelist/blacklist config card is no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the file/import action rail now also lives in `src/components/dataImporter/DataImporterFileActions.jsx`, so file picker wiring, import trigger, saved/deleted-list actions, and the mode label are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the saved-filter preset rail now also lives in `src/components/dataImporter/DataImporterFilterPresetControls.jsx`, so preset selection, apply/save/overwrite/delete/refresh actions, preset error feedback, and applied-preset status are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the main query/date/C/O filter group now also lives in `src/components/dataImporter/DataImporterQueryFilterControls.jsx`, so quick search, date presets/range, missing-staff/team toggles, and C/O threshold controls are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the duplicate workflow rail now also lives in `src/components/dataImporter/DataImporterDuplicateWorkflowControls.jsx`, so duplicate-filter toggling, duplicate-resolution entry, duplicate-plan summary text, and the auto-reconcile CTA are no longer rendered inline between the preset rail and grid toolbar in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the ECUS auto-sync config/status card now also lives in `src/components/dataImporter/DataImporterSyncConfigPanel.jsx`, so sync status badges, sync-config form fields, and the nested preview/run controls are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the control-region wrapper now also lives in `src/components/dataImporter/DataImporterListControlsPanel.jsx`, so the section shell, edit toggles, column-config affordance, saved-mode empty-query hint, and composition of the filter/preset/duplicate/grid/selection child controls are no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the team/staff/agency assignment popovers now also live in `src/components/dataImporter/DataImporterAssignmentComboboxes.jsx`, so the reusable roster/agency combobox JSX and search-reset logic are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the declaration-status badge/detail branch now also lives in `src/components/dataImporter/DataImporterDeclarationStatus.jsx`, with shared status resolution moved to `src/components/dataImporter/dataImporterDeclarationStatus.js`, so status labels, timestamp/detail formatting, and badge rendering are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the editable-row diff and roster normalization helpers now also live in `src/components/dataImporter/dataImporterRowUtils.js`, so `collectEditableDiff()` and `buildRosterTeams()` are no longer embedded near the top of `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the duplicate-diff grouping/formatting helpers now also live in `src/components/dataImporter/dataImporterDuplicateDiffUtils.js`, so duplicate field grouping, label humanization, temporal formatting, multiline joining, and leftover-field handling are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the duplicate-review timestamp/source/status/label and merge/sort helpers now also live in `src/components/dataImporter/dataImporterDuplicateReviewUtils.js`, so duplicate candidate weighting, merge-field application, review-flag cleanup, source inference, declaration labeling, and history timestamp formatting are no longer embedded near the top of `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the license/C/O normalization helpers now also live in `src/components/dataImporter/dataImporterLicenseUtils.js`, so license count coercion, manual override sync, code-list parse/join, agency key extraction, and `ensureCOFields()` are no longer embedded near the top of `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the importer config/state helper cluster now also lives in `src/components/dataImporter/dataImporterConfig.js`, so table/aux column labels, config options, width sanitation, view-mode/page-size constants, sync MST textarea parsing, date-range presets, and duplicate-merge field metadata are no longer embedded at the top of `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the ECUS sync workflow state now also lives in `src/components/dataImporter/useDataImporterSync.js`, so config/status/alert fetches, preview/run mutations, MST filter notices, and post-run reload orchestration are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the C/O monitoring/config workflow state now also lives in `src/components/dataImporter/useDataImporterCoMonitoring.js`, so code-config syncing, discrepancy config/status refresh, run/save mutations, and mismatch preview state are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the saved-session loader now also lives in `src/components/dataImporter/useDataImporterSavedSession.js`, so loading persisted declarations, resetting saved-mode UI state, auto-entering saved mode on mount, duplicate-filter reset on mode exit, and unsaved `beforeunload` guarding are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the file-ingest/manual-import workflow state now also lives in `src/components/dataImporter/useDataImporterImportFlow.js`, so XLSX validation/parsing, preview-mode reset, and persisted import/log/refresh orchestration are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the saved-mode save ownership now also lives in `src/components/dataImporter/useDataImporterSavedEdits.js`, so bulk save and single-row save/update normalization/history refresh are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the destructive row-mutation workflow now also lives in `src/components/dataImporter/useDataImporterRowMutations.js`, so soft delete, hard delete, restore, and the related selected-row destructive actions are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the review-alert workflow now also lives in `src/components/dataImporter/useDataImporterReviewActions.js`, so alert refresh plus reviewed/unreviewed selected-row actions are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the duplicate-diff state/derivation workflow now also lives in `src/components/dataImporter/useDataImporterDuplicateDiff.js`, so diff dialog open/close/swap/change handlers plus derived group/base/compare labels and changed-field counts are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the duplicate-review controller now also lives in `src/components/dataImporter/useDataImporterDuplicateReview.js`, so duplicate plan defaults, keeper/merge/resolution/note changes, review dialog open/close, and confirm reconciliation/delete persistence are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the license-exclusion reconciliation workflow now also lives in `src/components/dataImporter/useDataImporterLicenseExclusions.js`, so selected-row/manual apply, filtered-row auto-apply, exclusion summary shaping, and KPI recalculation after license-code exclusion are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the saved filter-preset workflow now also lives in `src/components/dataImporter/useDataImporterFilterPresets.js`, so preset payload building, preset apply/save/overwrite/delete flows, last-applied preset restore, legacy localStorage import, and clear-range handling are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the query/date/C/O filter controller now also lives in `src/components/dataImporter/useDataImporterQueryFilters.js`, so date-preset apply, clear-search-range behavior, and prop wiring for quick search, missing-staff/team toggles, and C/O threshold controls are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the roster-based assignment controller now also lives in `src/components/dataImporter/useDataImporterAssignments.js`, so staff/team/agency selection callbacks and their row-update shaping are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the selection/shared-row-action controller now also lives in `src/components/dataImporter/useDataImporterSelectionActions.js`, so filtered-selection derivation, reviewed-count derivation, per-row toggle selection, clear selection, and select-filtered behavior are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the selected-row bulk-action/export controller now also lives in `src/components/dataImporter/useDataImporterSelectionBulkActions.js`, so bulk capability derivation, selected-row XLSX export shaping, and the `selectionActionsProps` bundle are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the list-preferences controller now also lives in `src/components/dataImporter/useDataImporterListPreferences.js`, so page-size/view-mode/frozen-column/show-deleted toggles, overwrite confirmation, and column-config dialog opening are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the deleted-rows dialog controller now also lives in `src/components/dataImporter/useDataImporterDeletedRows.js`, so dialog open/close state, hard-deleted fetch/retry flow, and abort-on-close behavior are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the row-history controller now also lives in `src/components/dataImporter/useDataImporterRowHistory.js`, so history expansion state, history refresh, and row-level history toggle behavior are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: baseline snapshot ownership now also lives in `src/components/dataImporter/useDataImporterBaselineSnapshot.js`, so saved-row snapshot refresh, single-row baseline commit, and editable diff-map derivation are no longer embedded directly in `DataImporter.jsx` or mutating through `useDataImporterSavedEdits.js`.
- Current `cng-7c8.9` slice: saved-row inline editing now also lives in `src/components/dataImporter/useDataImporterRowEditing.js`, so `applyEdit()` owns editable-row guards, sanitized row merging, KPI recalculation, selected-key remap on identity changes, and server-search row patching instead of keeping that mutation-heavy callback inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: saved-mode result-row orchestration now also lives in `src/components/dataImporter/useDataImporterResultRows.js`, so server-side search fetches, client/server paging derivation, page reset behavior, and deleted-row selection pruning are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: shared row presentation helpers now also live in `src/components/dataImporter/useDataImporterRowPresentation.js`, so `buildRowState()`, card license-count normalization, and card C/O + KPI display formatters are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: saved-mode highlight derivation now also lives in `src/components/dataImporter/useDataImporterSavedHighlights.js`, so last-sync totals, updated-row preview state, and saved-mode highlight selection shortcuts are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: duplicate-group aggregation now also lives in `src/components/dataImporter/dataImporterDuplicateSummary.js`, so duplicate counting, keeper/removal selection, and review-dialog metadata shaping are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: persisted column-config and resize orchestration now also live in `src/components/dataImporter/useDataImporterColumnConfig.jsx`, so hidden-column hydration, resize persistence, sensitive-column gating, and apply/reset flows are no longer embedded directly in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the role/team edit-access seam now also lives in `src/components/dataImporter/useDataImporterEditAccess.js`, so admin/lead/staff gating, roster-team lookup, blocked-edit notice reset, and staff team-change sanitization are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: deleted-row summary shaping now also lives in `src/components/dataImporter/dataImporterDeletedEntries.js`, so deleted-entry fallback/sort/range-label logic is no longer embedded inline in `DataImporter.jsx`, and the parent now consumes `agencyOptions` directly from `useDataImporterEditAccess.js` instead of rebuilding the same option list locally.
- Current `cng-7c8.9` slice: duplicate-diff row preparation now also lives in `src/components/dataImporter/dataImporterDuplicateDiffPreparation.js`, so license snapshot shaping, timestamp label formatting, C/O summary fields, and deduped agency display are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: importer overview derivation now also lives in `src/components/dataImporter/useDataImporterOverview.js`, so saved-mode summary stats/cards, outstanding-alert preview, and last alert/sync labels are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: deleted-row dialog derivation now also lives in `src/components/dataImporter/useDataImporterDeletedRows.js`, so filtered hard-deleted rows, merged deleted entries, deleted counts, and date-range label are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: display/session preferences now also live in `src/components/dataImporter/useDataImporterDisplayPreferences.js`, so page-size hydration, view-mode persistence, frozen-column preference, and card-grid column persistence are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: sync-status toast wiring now also lives in `src/components/dataImporter/useDataImporterSyncStatusToast.js`, so storage-limit toast dedupe/subscription cleanup is no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: container width observation now also lives in `src/components/dataImporter/useDataImporterContainerWidth.js`, so `ResizeObserver` / window-resize fallback wiring is no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: action guards now also live in `src/components/dataImporter/useDataImporterActionGuards.js`, so editable/hard-delete eligibility filtering plus user-facing restriction alerts are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: license exclusion summary derivation now also lives in `src/components/dataImporter/useDataImporterLicenseSummary.js`, so rule-derived exclusion sets plus row-level license snapshot shaping are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: import-preview and overwrite coordination now also live in `src/components/dataImporter/useDataImporterImportPreview.js`, so 11-char preview truncation, upload/overwrite capability derivation, preview error fallback, and overwrite reset are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: saved-mode summary cards now also live in `src/components/dataImporter/DataImporterSummaryCards.jsx`, so the overview stat grid is no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: last-sync summary card now also lives in `src/components/dataImporter/DataImporterLastSyncSummaryCard.jsx`, so the latest sync recap block passed into `DataImporterSyncConfigPanel` is no longer constructed inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: updated-row highlight banner now also lives in `src/components/dataImporter/DataImporterUpdatedRowsBanner.jsx`, so the admin-only post-sync selection banner is no longer rendered inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: sync/admin prop assembly now also lives in `src/components/dataImporter/dataImporterSyncPanelProps.js`, so sync status tone mapping plus `DataImporterSyncConfigPanel` / C/O monitoring prop shaping are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: file/list/grid prop assembly now also lives in `src/components/dataImporter/dataImporterShellProps.js`, so import mode labels, duplicate workflow CTA props, grid-toolbar props, and list-controls props are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: result-surface prop assembly now also lives in `src/components/dataImporter/dataImporterResultsProps.js`, so the shared/table/card result prop bundles are no longer embedded inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: duplicate/reconcile cross-hook orchestration now also lives in `src/components/dataImporter/useDataImporterDuplicateWorkflow.js`, so duplicate summary aggregation, saved-mode highlight shortcuts, diff dialog state, and duplicate review/reconciliation plan wiring are no longer composed inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the top-level importer shell now also lives in `src/components/dataImporter/DataImporterShell.jsx`, so dialog/panel/banner/render branching for the import surface is no longer rendered inline in `DataImporter.jsx`, which now sits at `1420` lines.
- Current `cng-7c8.9` slice: the results-surface controller now also lives in `src/components/dataImporter/useDataImporterResultsController.js`, so row editing, assignment callbacks, destructive row actions, selection state, layout wiring, and table/card/bulk-action prop composition are no longer orchestrated inline in `DataImporter.jsx`.
- Current `cng-7c8.9` slice: the saved/import/sync session controller now also lives in `src/components/dataImporter/useDataImporterWorkflowSession.js`, so saved-row loading, sync state wiring, import execution wiring, review-action wiring, overview derivation, and range-preset coordination are no longer orchestrated inline in `DataImporter.jsx`, which now sits at `1223` lines.
- Current `cng-7c8.9` slice: the remaining result-surface orchestration now also lives in `src/components/dataImporter/useDataImporterResultsSurface.jsx`, so row search, unsaved-diff sync, save/edit delegation, license-exclusion actions, tooltip wiring, and last-sync card composition are no longer owned directly by `DataImporter.jsx`; the parent is now down to `768` lines and `useDataImporterSessionController.js` has been compacted to `799` lines.
- Current `cng-7c8.9` slice: the stale server-search overwrite regression has been fixed in `src/components/dataImporter/dataImporterSessionDerivedState.js` by separating filter-derived state from row-derived state, so local staff/team edits no longer retrigger an unnecessary server-search refresh that patches stale row data back over the inline edit.
- Current `cng-7c8.9` slice: workbook parsing now routes through `src/components/dataImporter/dataImporterWorkbookParser.js` with `src/components/dataImporter/dataImporterWorkbook.worker.js`, so `useDataImporterImportFlow.js` no longer performs `xlsx.read()` / `sheet_to_json()` on the main thread when `Worker` support exists, while sync fallback preserves older-browser and test stability.
- Current `cng-7c8.9` slice: the first guided-workflow shell now also lives in `src/components/dataImporter/DataImporterWorkflowGuide.jsx`, so `DataImporterShell.jsx` renders an explicit 3-step import/review/save progress surface via `dataImporterShellProps.js` / `dataImporterContainerProps.js` instead of leaving import/sync progress implied only by scattered panels; `DataImporter.jsx` is now down to `737` lines.
- Current `cng-7c8.9` slice: the guided-workflow shell now also exposes next-step CTA wiring via `src/components/dataImporter/DataImporterWorkflowGuide.jsx` and `src/components/dataImporter/dataImporterShellProps.js`, so operators can move through `Chọn file XLSX`, `Import XLSX`, and `Lưu dữ liệu` directly from the stepper without changing the underlying session/import/save logic.
- Current `cng-7c8.9` slice: the guided-workflow shell now also uses shared workflow state from `src/components/dataImporter/dataImporterWorkflowGuideState.js`, and `DataImporterShell.jsx` is split into explicit stage regions for `Bước 1: Nạp nguồn`, `Bước 2: Rà soát dữ liệu`, and `Bước 3: Lưu và theo dõi`, so sync/file actions, preview review surfaces, and save/follow-up surfaces are grouped by stage instead of remaining a flat panel stack.
- Current `cng-7c8.9` slice: the guided-workflow step cards now deep-link to those source/review/save regions via shared `targetId` state in `src/components/dataImporter/dataImporterWorkflowGuideState.js` and stable stage ids in `src/components/dataImporter/DataImporterShell.jsx`, so the stepper is now a real navigation surface instead of a passive status card.
- Current `cng-7c8.9` slice: the guided-workflow CTA rail now also surfaces the ECUS path via `Xem trước ECUS`, `Đồng bộ ngay`, and `Làm mới xem trước ECUS` in `src/components/dataImporter/DataImporterWorkflowGuide.jsx` and `src/components/dataImporter/dataImporterShellProps.js`, so sync preview/run no longer live only inside the sync config panel.
- Current `cng-7c8.9` slice: ECUS preview rows now promote into the same shared review/save flow as file imports via `previewSource="sync"` in `src/components/DataImporter.jsx`, `src/components/dataImporter/useDataImporterWorkflowSession.js`, and `src/components/dataImporter/useDataImporterImportPreview.js`, so sync preview is no longer trapped inside the source panel.
- Current `cng-7c8.9` slice: `src/components/dataImporter/DataImporterSyncPreviewPanel.jsx`, `src/components/dataImporter/DataImporterWorkflowGuide.jsx`, `src/components/dataImporter/dataImporterWorkflowGuideState.js`, `src/components/dataImporter/dataImporterShellProps.js`, `src/components/dataImporter/DataImporterImportPreviewSummary.jsx`, and `src/components/dataImporter/DataImporterListControlsPanel.jsx` now render sync-specific handoff messaging, review labels, and next-step actions once ECUS preview enters the shared review stage.
- Current `cng-7c8.9` slice: `src/components/dataImporter/useDataImporterSessionController.js` has been compacted back to `797` lines, keeping the remaining primary importer controllers under the project soft cap while preserving behavior.
- Current `cng-i6h.4` slice: generic `/api/storage` controller logic now lives in `server/storageRouteController.js`, so permission checks, blocked-key policy, actor forwarding, and HTTP/error shaping are no longer inline in `server/index.js`; the monolith keeps only route registration for that surface.
- Current `cng-i6h.4` slice: `server-v4` runtime business reads now go through `server-v4/src/persistence/sqliteBusinessSnapshotReader.ts`, so the canonical runtime no longer depends on `server-v4/src/legacy/legacy-business-snapshot-reader.ts` or `LegacyKvStoreReader` just to reach typed SQLite snapshots.
- Current `cng-i6h.4` slice: `mst-assignments` is now the first true async business-read seam on the Postgres cutover path. `server-v4/src/modules/mst-assignments/mstAssignmentAsyncReader.ts` and `postgresMstAssignmentAsyncReader.ts` let the canonical runtime read MST assignment rows from Postgres while the rest of the business snapshot surface remains on the SQLite dual-write compatibility reader.
- Current `cng-i6h.4` slice: `server-v4/src/app/build-v4-app.ts` now wires `mst-assignments` outside the sync `BusinessSnapshotReader` router factory map, so `buildV4App()` can mix one async Postgres-backed business route family with the remaining sync SQLite compatibility modules without reopening the rest of the runtime contract yet.
- Current `cng-i6h.4` slice: `teams` is now the second true async business-read seam on the Postgres cutover path. `server-v4/src/modules/teams/teamRosterAsyncReader.ts` and `postgresTeamRosterAsyncReader.ts` let the canonical runtime read typed team roster rows from Postgres, while `ReportingRepository` now awaits the same roster seam instead of calling the sync `TeamsRepository` path directly.
- Current `cng-i6h.4` slice: `declarations` is now the third async business-read seam on the Postgres cutover path. `server-v4/src/modules/declarations/declarationAsyncReader.ts` and `postgresDeclarationAsyncReader.ts` let both `/api/v4/declarations` and the reporting aggregate path consume Postgres-backed declaration rows, because `buildV4App()` now passes explicit async declaration/team readers into reporting instead of relying on sync fallback wrappers there.
- Current `cng-i6h.4` slice: `kpi-rules` is now the fourth async business-read seam on the Postgres cutover path. `server-v4/src/modules/kpi-rules/kpiRulesAsyncReader.ts` and `postgresKpiRulesAsyncReader.ts` let both `/api/v4/kpi-rules` and the reporting aggregate path consume Postgres-backed rule sets from `kpi_rule_sets`, because `buildV4App()` now mounts `kpi-rules` through `persistence.kpiRulesReader` and passes the same async rules seam into reporting.
- Current `cng-i6h.4` slice: `kpi-adjustments` is now the fifth async business-read seam on the Postgres cutover path. `server-v4/src/modules/kpi-adjustments/adjustmentAsyncReader.ts` and `postgresAdjustmentAsyncReader.ts` let the reporting aggregate path consume Postgres-backed typed adjustment snapshot rows, because `runtimePersistence` now exposes `persistence.adjustmentsReader` and `ReportingRepository` no longer calls the sync `readAdjustmentRows()` compatibility path directly.
- Current `cng-i6h.4` slice: `server-v4/src/modules/kpi-adjustments/postgresAdjustmentAsyncReader.ts` now prefers canonical `kpi_adjustments` rows before falling back to `adjustment_snapshot_rows` and then SQLite compatibility, so reporting adjustments are no longer limited to snapshot-row transport when relational adjustment rows exist in Postgres.
- Current `cng-i6h.4` slice: `server-v4/src/modules/declarations/postgresDeclarationAsyncReader.ts` now prefers canonical `declarations` plus `declaration_license_codes` rows before falling back to `declaration_snapshot_rows` and then SQLite compatibility, so declaration/reporting reads are no longer limited to Postgres snapshot-row transport when relational declaration rows exist.
- Current `cng-i6h.4` slice: `server-v4/src/modules/reporting/ReportingRepository.ts` and `server-v4/src/modules/reporting/reportingRoutes.ts` now require explicit async declaration/rule/adjustment/team readers from app composition, so the reporting boundary no longer accepts a raw sync `BusinessSnapshotReader` dependency just to build fallback wrappers.
- Current `cng-i6h.4` slice: `server-v4/src/persistence/runtimePersistence.ts` no longer exposes `reader` on the public `RuntimePersistence` contract, so `buildV4App()` custom persistence fixtures and route wiring no longer need to carry a raw sync SQLite reader outside the persistence layer.
- Current `cng-i6h.4` slice: `auth` is now mounted as a canonical `server-v4` read/write surface through `server-v4/src/modules/auth/*` plus `persistence.authStore`, so `/api/v4/auth/{session,login,logout,accounts}` no longer remains metadata-only and the default rollout catalog now has zero scaffold modules.
- Current `cng-i6h.4` slice: `kpi-adjustments` is now the third true `server-v4` write-owned business surface alongside `reporting` plus `auth`. `server-v4/src/modules/kpi-adjustments/kpiAdjustmentsRoutes.ts` now mounts `GET/PUT /settings`, `POST /`, and `PATCH /:adjustmentId`; `server-v4/src/persistence/runtimePersistence.ts` now exposes `adjustmentsStore` in both SQLite dual-write and Postgres modes; and the default rollout truth now sits at `5` read-only modules, `3` read-write modules, `24` implemented routes, and `9` implemented mutation routes.
